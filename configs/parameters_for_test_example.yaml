# This is just a template. Make a copy and remove the "_example" from its name for actual use.
# Do not edit this file.

arguments:
  zero_correction:
    # original arguments
    config_path: configs/xtc_config.json
    config_name: xtc_p16_phy
    detector_index: 36
    trigger_index: 50
    neg_matrix: results/overall/neg_restrained_xtalk_matrix.csv
    pos_matrix: results/overall/pos_restrained_xtalk_matrix.csv
    out_dir: results/zero_correction
    # cuts
    trigger_max: 9999
    trigger_threshold: 50
    response_min: -10
    response_max: 10
    bins: 200

